#!/bin/bash

settings='
{ evaluate: /<%([\s\S]+?)%>/g,
  interpolate: /<%=!([\s\S]+?)%>/g,
  escape: /<%=([\s\S]+?)%>/g }'

if [ "$1" == "watch" ]; then
  watch="-w"
fi
templito ./template ./js/template -n Templito -s "$settings" $watch
